[project]
name = "mifid-bonds-data-loader"
version = "0.1.0"
description = "Download, parse, and deduplicate ESMA FIRDS bond reference data into SQLite"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "httpx",
  "rich",
]
